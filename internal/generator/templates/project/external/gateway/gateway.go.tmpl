package gateway

import "go.uber.org/fx"

const httpClientName = `name:"gatewayHTTPClient"`

var Module = fx.Provide(
	provideConfig,
	fx.Annotate(provideHTTPClient, fx.ResultTags(httpClientName)),
	fx.Annotate(NewClient, fx.ParamTags(httpClientName, "")),
)
