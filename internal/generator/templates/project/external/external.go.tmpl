package external

import (
{{- if .WithGateway}}
	"{{.Module}}/external/gateway"
{{- end}}
	// svcgen:imports

	"go.uber.org/fx"
)

var Module = fx.Options(
{{- if .WithGateway}}
	gateway.Module,
{{- end}}
	// svcgen:modules
)
