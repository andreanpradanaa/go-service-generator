package router

import (
	"{{.Module}}/internal/router/exception"
	"{{.Module}}/internal/router/filter"

	"go.uber.org/fx"
)

var Module = fx.Options(
	exception.Module,
	filter.Module,
	routerModule,
)

var routerModule = fx.Provide(
	fx.Annotate(newHealthRouter, fx.ResultTags(`group:"routes"`)),
	// svcgen:routes
)
